# ことば推理ゲーム仕様書（v1）

## 1. 概要

2人・3人・4人で遊ぶ対面ローカルの連想・推理ゲーム。各プレイヤーは、手札から選んだ頭文字と条件文を満たす秘密の単語を決める。判定結果を手がかりに、他プレイヤーの秘密の単語を回答して得点を得る。

- 秘密の単語を当てる行為を「回答」、その機会を「回答権」と呼ぶ。
- 判定カードへの `YES` / `NO` / `？` の入力は「判定回答」と呼び、回答権とは区別する。
- v1では、対面ローカルプレイを対象とする。チュートリアルと開発者用カード・デッキ編集は、対面ローカルモード完成後に実装する。

## 2. ゲーム開始前の設定

対面ローカルプレイを選ぶと、ゲーム開始前の設定画面を表示する。一般ユーザーは次の項目を選択できる。

| 設定 | 選択肢 | 内容 |
| --- | --- | --- |
| プレイ人数 | 2人 / 3人 / 4人 | プレイヤー数を決める。 |
| セット数 | 1〜4セット | ゲーム全体で行うセット数を決める。 |
| 自分への判定 | 有効 / 無効 | 手番プレイヤー自身についても判定を行うかを決める。 |
| 文字数公開 | 有効 / 無効 | 指定時点で秘密単語の文字数を公開するかを決める。 |
| ひらがな位置公開 | 有効 / 無効 | ひらがな判定で `YES` になった文字の位置を、文字数公開後に表示するかを決める。 |
| 不正解の回答語公開 | 有効 / 無効 | 不正解だった回答語を表示するかを決める。 |
| 被正解者の得点方式 | 通常 / 減点 | 正解されたプレイヤーの得点処理を決める。 |

- 設定を確定すると、秘密単語設定へ進む。
- 設定変更はゲーム開始前だけ可能とする。ゲーム開始後は変更できない。
- 将来、設定の組み合わせをお気に入りとして3パターンまで保存し、ワンクリックで開始できるようにする。

## 3. カード、デッキ、秘密の単語

### 3.1 v1の標準デッキ

- v1では、現在の44枚を標準デッキとして使用する。
- 各プレイヤーは手札5枚を受け取る。
- 各カードには、頭文字と条件文が記載されている。
- 各プレイヤーは手札から異なる2枚を選ぶ。
  - 1枚の頭文字を秘密単語の頭文字にする。
  - 別の1枚の条件文を秘密単語の条件にする。
- 1枚のカードから頭文字と条件文を同時に選ぶことはできない。片側を選ぶと、そのカードのもう片側は選べない。

### 3.2 カードの入力規則

- v1のカード項目は、カードID、頭文字、条件文とする。
- 頭文字には通常のひらがなに加え、が行・ざ行・だ行・ば行・ぱ行の濁点・半濁点付きひらがなを使用できる。
- 小書き文字、「ゔ」などの例外的な文字は頭文字に使用できない。
- 条件文は空欄を不可とし、空白を含めて4文字以上20文字以下とする。
- 条件文に絵文字は使用できない。
- 同じ頭文字、同じ条件文、または両方が同じカードの重複を許可する。ただし、頭文字と条件文の組み合わせが完全に同じカードを追加する際は、重複を表示して開発者の確認を要求する。

### 3.3 秘密単語の入力

- 秘密単語は、選んだ頭文字で始まり、選んだ条件文を満たすものを本人が入力する。
- 単語の途中には、濁点・半濁点付き文字、小文字を含めてよい。
- 拗音が単語の先頭にある場合は、基となる通常のひらがなを頭文字として扱う。
- 秘密単語の入力には端末標準のソフトキーボードを使用する。
- 自動補正を無効化し、前後の空白を除去して保存する。
- キーボードの完了操作だけでは確定せず、画面上の確定ボタンで設定を完了する。

## 4. 秘密単語の設定とセット開始

1. 山札をシャッフルし、各プレイヤーへ5枚ずつ配る。
2. 全プレイヤーの手札と、それぞれの「秘密単語を設定する」ボタンを表示する。
3. 各プレイヤーは任意の順番で自分のボタンを押し、頭文字・条件文・秘密単語を設定する。
4. 設定を完了すると、全プレイヤーの設定状況を表示する画面へ戻る。
5. 秘密単語を設定済みにしたプレイヤーは、設定内容を修正できない。
6. 全員の設定完了後、頭文字があいうえお順で最も早いプレイヤーを先手とする。同順位はランダムで決める。
7. 頭文字と条件文を判定記録表の公開情報として表示し、ゲームを開始する。

得点だけを次セットへ持ち越す。手札、山札、回答権、判定記録、秘密単語はセットごとにリセットする。

## 5. 手番数、文字公開、セット終了

| モード | プレイヤー数 | 1人あたりの手番 | 合計手番 | 文字数公開時点 |
| --- | ---: | ---: | ---: | --- |
| 2人モード | 2人 | 5回 | 10回 | 第5手番開始時（第4手番終了後） |
| 3人モード | 3人 | 4回 | 12回 | 第4手番開始時（第3手番終了後） |
| 4人モード | 4人 | 3回 | 12回 | 第5手番開始時（第4手番終了後） |

- 手番は先手から順にプレイヤーを循環する。
- 合計手番を終えるか、全員が正解済みになった時点でセットを終了する。
- 「文字数公開」が有効の場合、表の時点で秘密単語の文字数を公開する。
- セット終了後は投票フェーズへ進む。
- 投票フェーズ終了後、最終セット以外では「次のセットへ」を押して秘密単語設定へ進む。最終セットでは次セットへ進まず、最終結果の待機状態とする。

## 6. 手番の流れ

1. 手番プレイヤーは自分の手札の頭文字領域または条件文領域を選ぶ。
2. 選んだ側を判定カードとして使用し、判定結果を記録する。
3. 手番プレイヤーは必要であれば回答権を1回使う。
4. 回答しない場合は「回答せず終了」を選ぶ。山札が残っていれば1枚補充し、次プレイヤーへ進む。

- 使用したカードは手札から除かれる。
- 山札が尽きた場合、補充は行わず次プレイヤーへ進む。
- 「回答せず終了」後の補充・次プレイヤー・次手番の見せ方は演出上の項目とし、v1では定めない。

## 7. 判定回答とUNDO

### 7.1 ひらがな判定

ひらがな判定はゲーム側が自動処理する。選択されたひらがなが、各プレイヤーの秘密単語に文字そのものとして含まれるかを判定する。

- 含まれる: `YES`
- 含まれない: `NO`
- 音の近さでは判定しない。例: 「やきゅう」に「ゆ」は含まれないため `NO`。
- 濁点・半濁点は正規化後の文字完全一致で判定する。
- ひらがな領域の判定カード選択はUNDOできない。

### 7.2 条件文判定

条件文判定は各プレイヤー本人が行う。

- 条件文判定が開始されたら、ゲームのメイン画面上に選択用画面をオーバーレイ表示する。
- 選択用画面では、各プレイヤーごとに領域を分け、`YES`、`NO`、`？` のボタンを表示する。
- 対面ローカルでは、他プレイヤーのボタン操作は見えてよい。ただし、選択後の回答内容は全員の入力完了まで表示しない。
- 全員の判定選択が完了した時点で、回答内容を一斉に公開する。
- 正解済みプレイヤーは以後も手番を行うが、判定回答はしない。以後の判定記録マスは空欄とする。
- 「自分への判定」が無効の場合は、手番プレイヤー自身の判定記録マスを空欄とする。

### 7.3 UNDO

対面ローカルでは、誤操作救済のため次のUNDOを提供する。

| 操作 | UNDOの扱い |
| --- | --- |
| 秘密設定中のカード選択、秘密単語入力、回答入力欄の文字入力 | 確定前なら常に変更できる。 |
| 条件文の判定カード選択 | 判定対象者の誰も判定選択をしていない場合に限り、判定カード未選択状態へ戻せる。 |
| 条件文の判定回答 | 判定対象者全員の判定選択が完了していない場合に限り、自分の判定選択を未選択へ戻せる。 |
| 回答送信、回答権の消費、得点変動、秘密単語の公開、手番終了と補充、文字数公開、セット終了・次セット開始、ゲーム設定変更 | UNDOできない。 |

- 条件文判定の選択画面では、UNDO操作を下部中央の「戻る」として表示する。

## 8. 判定記録表とゲーム画面

判定記録表は、左端の公開情報列、中央の判定列、右端の公開列・操作列で構成する。

- 左端には頭文字と条件文を表示する。文字数公開後は、左寄せで単語マスクを表示する。
- 判定表の各行とプレイヤー名の対応は公開情報とする。
- 中央には各手番の判定結果を表示する。上段には使用された頭文字または条件文を表示する。条件文は縦書きで、長い場合は最大2列に折り返す。
- 右端の公開列には、正解時の秘密単語と、セット終了時の全秘密単語を中央寄せで表示する。正解によって公開された単語のマスだけを薄いピンク色で表示する。
- 右端の操作列には「回答する」と「回答せず終了」を表示する。
- 各プレイヤーの残り回答権は、そのプレイヤーの手札付近に表示する。
- 各プレイヤーに固有の色を割り当て、手札領域、文字列、操作領域などへ一貫して使用する。

## 9. 文字数と文字位置の公開

- 文字数は濁点・半濁点付き文字を1文字、小文字を1文字として数える。
- 公開時は先頭文字を表示し、残りを `○` で隠す。例: 「こくばん」は `こ○○○`。
- 「ひらがな位置公開」が有効の場合、ひらがな判定で `YES` になった文字の全位置を該当ひらがなへ置き換える。例: 「こくばん」で「ば」が `YES` になった場合は `こ○ば○`。
- 先頭文字と判定で公開された文字は、本来の1文字分の `○` を置き換える。表示の総文字数は秘密単語の文字数と常に一致する。

## 10. 回答権と得点

- 各プレイヤーの回答権は1セットにつき2回。
- 自分の手番で、未正解の相手プレイヤー行の「回答する」を押して回答権を使う。
- 同じ相手を複数回の回答対象にしてよい。
- 1手番に行使できる回答権は最大1回。
- 回答入力画面には「戻る」を表示する。「戻る」は回答権を消費せず、手番も終了せず、補充も行わず、回答操作前の手番画面へ戻る。
- 回答入力には端末標準のソフトキーボードを使用する。キーボードの完了操作では回答を送信せず、画面上の「回答する」で送信する。
- 回答送信前の確認画面は表示しない。送信すると正解・不正解を判定したうえで、ゲームのメイン画面へ自動的に戻る。
- 正解時は回答者が1点を得る。被正解者は、得点方式に従い通常では1点を得て、減点では1点を失う。得点は0点未満になり得る。
- 正解された単語は、対象プレイヤーの公開列に表示する。
- 不正解時は回答権だけを1回消費する。追加ペナルティはない。
- 不正解の回答語公開が有効の場合は、回答語の文字列だけを表示する。無効の場合は「不正解」だけを表示する。
- 不正解時の回答者は、表示する文字列を回答者の色で示す。回答対象者は、その文字列の判定記録表上の表示位置で示し、文字列にプレイヤー名を含めない。

## 11. セット終了時の投票フェーズ

セット終了後、次セットへ進む、または最終結果へ進む前に投票フェーズを行う。

- 投票フェーズは2人・3人・4人の全モードで行う。
- 手番数上限による終了時だけでなく、全員正解による早期終了時にも行う。
- プレイ人数と同数の投票を行う。対面ローカルでは、各プレイヤーが1回ずつ選ぶことをルール上の指示とし、ゲーム処理として投票者の識別・回数制限は行わない。
- 各プレイヤーは自分以外の秘密単語から、良いと思う単語を1つ選ぶ。
- 同じ秘密単語へ複数人が投票できる。
- 投票中は、投票先の所有者名と各秘密単語の途中得票数を表示する。
- 1票ごとに、選ばれた秘密単語の所有者へ0.5点を加算する。
- 必要な投票数に達するまで、次セットへの遷移または最終結果の表示へ進めない。

## 12. 端末の戻る操作

| 状態 | 戻る操作の扱い |
| --- | --- |
| ソフトキーボード表示中 | キーボードだけを閉じる。 |
| 回答入力・条件文判定などの画面 | 端末戻るでは画面を離脱しない。画面上の「戻る」だけを有効な操作とする。 |
| メインゲーム画面 | ゲームを終了するか確認する。キャンセル時はゲームを継続する。 |
| 終了を確定 | 途中ゲームを破棄して起動画面へ戻る。 |

- v1では、アプリ終了・ブラウザ再読み込み後の途中ゲーム再開には対応しない。
- 途中ゲーム保存と起動時の再開機能はv1.x以降に扱う。
- 秘密単語設定時の覗き見防止画面・演出は実装しない。対面プレイでは、各プレイヤーが端末を適切に扱うものとする。

## 13. 今後の拡張

- チュートリアルは固定シナリオのNPCとの1対1とする。対面ローカルモード完成後に、その画面構成を参照して実装する。
- 開発者用のカードプール・デッキ編集画面はv1の優先課題だが、画面構成と操作詳細は対面ローカルモード完成後に再検討する。
- オンライン対戦、AI NPC対戦、カード固有効果、カード枚数44枚以外の標準デッキは将来の拡張とする。詳細は「開発構想・ロードマップ.md」を正とする。

## 14. 既知の検証状況

- 現行の `index.html` は旧仕様の単体Webアプリであり、本仕様への完全な追従は未実施である。
- Flutter版v1は未実装である。
